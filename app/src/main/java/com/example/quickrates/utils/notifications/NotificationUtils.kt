package com.example.quickrates.utils.notifications

import android.app.NotificationManager
import androidx.core.app.NotificationCompat

object NotificationUtils {
    const val NOTIFICATION_ID = "rates_channel"
    const val NOTIFICATION_NAME = "Main Channel"
    const val NOTIFICATION_IMPORTANCE = NotificationManager.IMPORTANCE_DEFAULT

    //Utils Notification Builder
    const val NOTIFICATION_PRIORITY = NotificationCompat.PRIORITY_DEFAULT
    const val NOTIFICATION_ID_NUMBER = 1
}